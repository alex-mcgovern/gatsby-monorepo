import { vars } from "../theme.css";

export const varsBorder = {
  accent_nonInteractive: `1px solid ${vars.color.accent_border_nonInteractive}`,
  accent_border_interactive: `1px solid ${vars.color.accent_border_interactive}`,
  accent_border_interactiveActive: `1px solid ${vars.color.accent_border_interactiveActive}`,

  neutral_nonInteractive: `1px solid ${vars.color.neutral_border_nonInteractive}`,
  neutral_border_interactive: `1px solid ${vars.color.neutral_border_interactive}`,
  neutral_border_interactiveActive: `1px solid ${vars.color.neutral_border_interactiveActive}`,
};
