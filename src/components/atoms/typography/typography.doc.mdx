---
title: Typography
mdxType: Component
atomicLevel: Atoms
categories: ["Polymorphic"]
description: "Polymorphic Typography component"
---

import { Box } from "../box/box.tsx";
import { TYPOGRAPHY_SIZE_VARIANTS_MOCK } from "./__mocks__/typography_size_variants.mock.ts";
import { Typography } from "./typography.tsx";

### `fontSize`

<Box customisation={{}}>
  {TYPOGRAPHY_SIZE_VARIANTS_MOCK?.length > 0 &&
    TYPOGRAPHY_SIZE_VARIANTS_MOCK.map((mock) => {
      return (
        <Box>
          <Typography as="code">{mock.fontSize}</Typography>
          <Typography {...mock}></Typography>
        </Box>
      );
    })}
</Box>
